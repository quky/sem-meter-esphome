# Backups

Keep local firmware dumps and private YAML backups here if you want them near the
project while developing.

This directory is intentionally ignored by Git except for this README. Do not
commit the original 16 MB factory firmware backup or any file containing Wi-Fi
credentials.
